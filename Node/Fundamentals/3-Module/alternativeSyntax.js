//Alternative Syntax to write module.exports but keep remember it's same thing that we have done before

// const items = ["item1", "item2", "item3", "item4"];
module.exports.items = ["item1", "item2", "item3", "item4"];

const person = {
  name: "Bob",
};

module.exports.singlePerson = person;
